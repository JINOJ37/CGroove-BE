# ===========================================
# Dance Community 환경 변수 설정
# ===========================================
# EC2에서 아래 환경변수를 설정하세요
#
# 방법 1: /etc/environment에 추가
# 방법 2: systemd 서비스 파일에 Environment= 추가
# 방법 3: 실행 시 export로 설정

# ===========================================
# 필수 설정 (운영 환경에서 반드시 설정)
# ===========================================

# JWT Secret Key (최소 32자 이상, 특수문자 포함 권장)
# 예: openssl rand -base64 32 로 생성
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-min-32-chars!

# 데이터베이스 비밀번호 (RDS 비밀번호)
DB_PASSWORD=your_rds_password

# CORS 허용 Origin (프론트엔드 도메인, 쉼표로 구분)
CORS_ALLOWED_ORIGINS=https://cgroove.vercel.app

# ===========================================
# 선택 설정 (기본값이 있어서 설정 안해도 됨)
# ===========================================

# Spring 프로파일 (dev, prod)
SPRING_PROFILES_ACTIVE=prod

# 파일 업로드 경로 (EC2 내 경로)
FILE_UPLOAD_BASE_DIR=/home/ec2-user/app/uploads
